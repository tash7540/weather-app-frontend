import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  card:{
    textAlign:'center',
    padding:'12px',
  },
  cardText:{
    fontSize:'20px',
    fontWeight:'bold',
  }

});
